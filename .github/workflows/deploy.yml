name: Automatically Deploy

on:
  push:
    branches:
    - main

jobs:
  heroku_1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          ref: main
          fetch-depth: 0
      - uses: ZeroKen5969/git-https-push-action@master
        name: heroku_1
        with:
          remoteUrl: '${{ secrets.HEROKU_1 }}'
          skipHooks: false
